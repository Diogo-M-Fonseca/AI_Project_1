using UnityEngine;

public enum Estados //enum where estados are stated for both for crew and robos
{
    Idle,
    Moving,
    Working,
    Resting,
    Collecting,
    Charging,
    RespondingToIncident,
    Evacuating,
    Dead
}
